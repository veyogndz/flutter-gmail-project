import 'package:flutter/material.dart';

class SpamKutusu extends StatelessWidget {
  const SpamKutusu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Spam Kutusu"),
      ),
    );
  }
}
