import 'package:flutter/material.dart';

class YildizliKutu extends StatelessWidget {
  const YildizliKutu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Yıldızlı"),
      ),
    );
  }
}
