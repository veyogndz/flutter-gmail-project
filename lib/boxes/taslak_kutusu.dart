import 'package:flutter/material.dart';

class TaslakKutusu extends StatelessWidget {
  const TaslakKutusu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Taslak Kutusu"),
      ),
    );
  }
}
