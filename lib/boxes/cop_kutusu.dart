import 'package:flutter/material.dart';

class CopKutusu extends StatelessWidget {
  const CopKutusu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Cöp Kutusu"),
      ),
    );
  }
}
